# 生态养殖系统功能描述

---

## 1. 项目概览

本系统采用前后端分离架构，用于养殖业务的数字化管理、生产数据采集、质量溯源和多角色协同。系统包含管理员、养殖户、兽医和公众四类使用场景，其中公众无需登录即可通过溯源码查询公开的批次信息。

系统业务主线为：

**养殖户注册与实名认证 → 养殖业务数据采集 → 生产过程管理 → 批次出栏 → 生成溯源码 → 公众查询溯源信息**。

管理员负责全局监管与审核，兽医负责疾病处理，养殖户负责自身养殖业务数据维护。

---

## 2. 技术架构

### 2.1 后端

| 技术 | 用途 |
| --- | --- |
| Python / Django 5.2 | Web 框架、ORM、认证和管理后台 |
| Django REST Framework | REST API、ViewSet、分页、过滤、搜索和排序 |
| djangorestframework-simplejwt | JWT 登录与 Token 刷新 |
| MySQL / PyMySQL | 业务数据持久化 |
| Django Channels + Daphne | WebSocket 实时刷新 |
| Pillow | 图片上传校验与缩略图处理 |
| qrcode | 生成批次溯源二维码 |
| Pandas / NumPy | 统计数据处理 |
| django-filter | API 条件筛选 |
| django-cors-headers | 前后端跨域控制 |

### 2.2 前端

| 技术 | 用途 |
| --- | --- |
| Vue 3 + TypeScript | 管理界面与公众溯源页面 |
| Vite | 开发服务器与生产构建 |
| Vue Router | 路由及角色访问控制 |
| Pinia | 用户状态与认证状态管理 |
| Axios | API 请求及 JWT 自动刷新 |
| Element Plus | UI 组件 |
| ECharts | 数据统计可视化 |
| WebSocket | 实时数据刷新 |
| qrcode.vue | 前端二维码展示 |
| dayjs / lodash-es / nprogress / sass | 日期处理、防抖节流、进度提示和样式 |

---

## 3. 系统运行方式

### 3.1 后端

开发环境：

```bash
python manage.py check
python manage.py runserver 0.0.0.0:8000
```

生产环境推荐使用 ASGI：

```bash
daphne -b 0.0.0.0 -p 8000 breeding_system.asgi:application
```

`breeding_system/asgi.py` 同时处理 HTTP 和 WebSocket 请求。

### 3.2 前端

```bash
cd frontend
npm install
npm run dev -- --host 0.0.0.0 --port 5173
```

默认访问地址：

| 项目 | 地址 |
| --- | --- |
| 前端管理界面 | `http://localhost:5173/` |
| 后端 API | `http://localhost:8000/` |
| Django 管理后台 | `http://localhost:8000/admin/` |
| WebSocket | `ws://localhost:8000/ws/refresh/` |
| 公众溯源页面 | 前端路由 `/public/trace/{trace_code}` |

---

## 4. 后端模块

```text
apps/
├── core/                  # 公共基础设施、分页、WebSocket、工具
├── users/                 # 用户、认证、个人中心、角色权限
├── livestock/             # 牲畜分类、养殖区域、批次
├── production/            # 饲料、投喂、疫苗、疾病
├── stats/                 # 管理员与养殖户统计
└── traceability_app/      # 公众溯源与后台溯源管理
```

### 4.1 用户模块

主要能力：

- 用户注册与登录
- JWT Access / Refresh Token
- 个人信息维护
- 密码修改
- 头像上传
- 养殖户实名认证申请
- 管理员审核实名认证
- 用户启用与禁用
- 角色识别与权限控制

主要接口：

```text
POST /api/users/register/
POST /api/users/login/
POST /api/users/token/refresh/
GET  /api/users/profile/info/
PATCH /api/users/profile/update_info/
POST /api/users/profile/upload_avatar/
POST /api/users/profile/change_password/
GET /api/users/farmer/
GET/POST /api/users/cert/
GET /api/users/cert/my_cert/
POST /api/users/cert/{id}/audit/
```

### 4.2 牲畜与批次模块

负责牲畜分类、养殖区域和批次信息管理。

主要能力：

- 牲畜分类维护
- 养殖区域管理
- 批次创建、查询、修改和删除
- 批次状态管理
- 批次数量维护
- 批次图片管理
- 批次出栏
- 出栏后创建溯源记录与二维码

主要接口：

```text
/api/livestock/category/
/api/livestock/area/
/api/livestock/batch/
```

### 4.3 生产管理模块

#### 饲料

记录饲料档案、库存和图片信息。

#### 投喂

投喂记录会联动饲料库存。关键库存写操作使用事务和行级锁，避免并发情况下出现库存超卖。

#### 疫苗

记录批次疫苗接种信息及后续接种日期。

#### 疾病

实现疾病上报、管理员分配、兽医处理和处理结果记录。疾病处理结果可以联动批次数量及状态。

主要接口：

```text
/api/production/feed/
/api/production/feeding/
/api/production/vaccination/
/api/production/disease/
```

### 4.4 统计模块

提供管理员和养殖户两套统计视图：

```text
GET /api/stats/admin/dashboard/
GET /api/stats/farmer/dashboard/
```

统计内容包括养殖批次、数量、库存、生产记录和趋势数据等，并由前端 ECharts 进行可视化展示。

### 4.5 溯源模块

批次出栏后生成唯一溯源码及二维码。公众通过溯源码可以匿名查询允许公开的信息，包括：

- 批次基本信息
- 牲畜分类
- 养殖区域
- 养殖户认证标识
- 投喂记录
- 疫苗记录
- 疾病处理记录
- 出栏信息

接口：

```text
GET /api/traceability/public/{trace_code}/
GET /api/traceability/admin/
DELETE /api/traceability/admin/{id}/
```

---

## 5. 角色与权限

### 5.1 管理员

管理员通过 `is_staff=True` 识别，拥有全局监管权限，包括：

- 用户管理
- 养殖户认证审核
- 疾病分配与监管
- 溯源记录管理
- 数据统计
- Django Admin 管理

### 5.2 养殖户

非管理员且不属于兽医分组的用户默认为养殖户。养殖户只能访问自己的业务数据，关键写操作需要通过实名认证。

### 5.3 兽医

用户属于 `vet` 分组且不是管理员时识别为兽医。兽医主要处理分配给自己的疾病上报。

### 5.4 公众

无需登录，通过溯源码访问公开溯源接口和页面，只能获取允许公开的数据。

---

## 6. 权限实现

系统通过自定义权限类和通用 ViewSet 基类实现业务访问控制。

主要权限：

| 权限类 | 规则 |
| --- | --- |
| `IsAdminUser` | 登录且为管理员 |
| `IsFarmerUser` | 登录、非管理员、非兽医 |
| `IsVerifiedFarmer` | 登录、非管理员且已通过实名认证 |
| `IsVetUser` | 登录、非管理员且属于 vet 分组 |
| `DenyAll` | 恒拒绝指定操作 |

核心 ViewSet：

- `BasePublicViewSet`：提供公共资源的统一权限、过滤和刷新机制。
- `BaseOwnerViewSet`：按所有权过滤数据，并统一处理创建、更新、删除权限。

列表 API 统一支持分页、搜索和排序，分页默认 10 条，最大 100 条。

---

## 7. 数据一致性与并发控制

饲料库存和批次数量属于关键业务数据，不能仅依赖前端校验。

系统在服务端使用数据库事务与行级锁处理关键写操作：

```text
开始事务
   ↓
锁定目标记录
   ↓
重新读取当前库存 / 数量
   ↓
校验业务条件
   ↓
更新数据
   ↓
提交事务
```

这样可以降低并发请求造成的数据竞争和库存超卖风险。

---

## 8. 实时刷新

后端使用 Django Channels + Daphne 提供 WebSocket 通道。

前端连接：

```text
ws://localhost:8000/ws/refresh/
```

主要机制：

- WebSocket 自动连接
- 断线自动重连
- 心跳保活
- 服务端发生数据变化后广播刷新事件
- WebSocket 不可用时使用轮询作为兜底

---

## 9. 图片与敏感数据

系统包含头像、认证材料、批次图片、饲料图片、区域图片和溯源二维码等媒体资源。

安全要求：

- 限制上传文件类型和大小
- 服务端重新校验上传文件
- 图片处理使用 Pillow
- 身份证等敏感字段采用加密存储
- 前端及接口根据角色进行脱敏展示
- 不在错误响应中返回内部异常、路径或 traceback

---

## 10. 配置与部署

项目通过 `.env` 管理环境配置，生产环境至少设置：

```text
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=实际域名或IP
DJANGO_SECRET_KEY=随机高强度密钥
DB_NAME=ecological_breeding_system
DB_USER=低权限数据库账号
DB_PASSWORD=数据库密码
DB_HOST=数据库地址
DB_PORT=3306
```

生产部署建议：

1. 使用 Nginx 作为反向代理。
2. 全站启用 HTTPS。
3. 使用 Daphne 等 ASGI 服务运行后端。
4. 多进程部署时使用 Redis 作为共享 Channel Layer。
5. CORS 只允许实际前端来源。
6. 不将 `.env`、数据库密码、Token 等敏感信息提交到 Git。
7. 媒体文件与应用代码分离管理。

---

## 11. 依赖与安全维护

项目依赖以 `requirements.txt` 和 `frontend/package.json` 为准。

近期已完成的安全维护包括：

- Django 升级至 5.2 系列安全版本。
- Pillow、Daphne、Requests、python-dotenv 完成安全更新。
- 前端依赖完成 Dependabot 安全更新。
- 头像上传异常信息不再直接返回客户端。
- CodeQL Python 与 JavaScript/TypeScript 检查通过相关修复。
- Django REST Framework 的剩余安全告警通过依赖升级处理，具体版本以仓库当前依赖文件和 GitHub Security 页面为准。

---

## 12. 项目目录速览

```text
DjangoProject/
├── manage.py
├── requirements.txt
├── .env.example
├── database/
│   └── ecological_breeding_system.sql
├── breeding_system/
│   ├── settings.py
│   ├── urls.py
│   ├── asgi.py
│   └── wsgi.py
├── apps/
│   ├── core/
│   ├── users/
│   ├── livestock/
│   ├── production/
│   ├── stats/
│   └── traceability_app/
├── media/
└── frontend/
    ├── package.json
    ├── vite.config.ts
    └── src/
        ├── components/
        ├── hooks/
        ├── layout/
        ├── router/
        ├── store/
        ├── utils/
        └── views/
```

---

## 13. 维护说明

开发人员修改业务功能时，应同步检查：

- API 权限是否正确
- 数据所有权是否正确
- 事务边界是否完整
- 上传文件是否经过校验
- 错误响应是否泄露内部信息
- WebSocket 失败时是否存在可接受的轮询兜底
- 依赖更新是否通过 CodeQL 和项目测试

文档中的依赖版本、接口行为和部署参数应以仓库当前代码为最终准据。
