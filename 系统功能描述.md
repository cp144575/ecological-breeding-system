# 生态养殖系统（毕业设计）功能描述

---

## 1. 项目概览

本项目为“生态养殖系统”，采用**前后端分离**架构，实现养殖业务的数字化管理、质量溯源查询与多角色协同（管理员、养殖户、兽医），并向公众提供无需登录的质量溯源查询能力。

- **后端**：Django + Django REST Framework（DRF）提供标准化 REST API；JWT 实现无状态认证；结合角色权限与认证状态实现业务访问控制；关键数据写入（饲料库存、批次数量）使用数据库事务与行级锁保障一致性；通过 Django Channels + Daphne 提供 WebSocket 实时刷新通道。
- **前端**：Vue 3 + TypeScript 构建管理界面与溯源展示页，Vue Router 实现路由级角色访问控制，Pinia 管理登录态，Axios 封装请求并自动携带/刷新 JWT，ECharts 展示统计图表，WebSocket 驱动列表“无刷新更新”。
- **公众端**：通过溯源码即可匿名查询批次全生命周期信息（批次档案、投喂/防疫/疾病历史、产地环境、养殖户认证标识）。

系统整体以“**养殖户实名认证 → 业务数据采集 → 批次出栏生成溯源码 → 公众扫码溯源**”为主线，辅以“**管理员监管 + 兽医协同处理疾病**”的闭环管理。

---

## 2. 主要技术栈

### 2.1 后端

| 技术 | 用途 |
| --- | --- |
| Python / Django 4.2 | Web 框架、ORM、认证、Admin |
| Django REST Framework 3.16 | REST API、ViewSet、分页、过滤、搜索、排序 |
| djangorestframework-simplejwt | JWT 登录 / 刷新（Access 1 天、Refresh 7 天） |
| MySQL（PyMySQL） | 业务数据存储（库名 `ecological_breeding_system`） |
| django-filter | 列表筛选（`filterset_fields`） |
| Django Channels + Daphne | WebSocket 实时刷新广播（开发默认内存 Channel Layer） |
| Pillow | 头像缩略图、图片上传校验 |
| qrcode | 批次出栏后生成溯源二维码图片 |
| Pandas | 统计看板按日重采样与补零 |
| django-cors-headers | 前后端跨域（开发期允许全部来源） |
| drf-spectacular | 已列入依赖（当前未在 Settings/URL 中启用） |

### 2.2 前端

| 技术 | 用途 |
| --- | --- |
| Vue 3 + TypeScript | 界面开发 |
| Vite 7 | 开发服务器与构建（开发端口 5173，代理 `/api`、`/media` 到后端 8000） |
| Vue Router 4 | 路由与角色访问控制（路由守卫） |
| Pinia | 用户状态、Token 管理、认证管理多 Tab 状态 |
| Axios | 请求封装、拦截器自动携带 JWT、401 自动刷新 Token（单飞刷新） |
| Element Plus | UI 组件库（含表单、弹窗、表格、进度、评分等） |
| ECharts | 统计可视化（趋势折线、库存饼图等） |
| WebSocket | 实时刷新消息订阅（自动重连 + 心跳 + 断线轮询补偿） |
| qrcode.vue | 前端二维码 / 溯源合格证展示 |
| dayjs / lodash-es / nprogress / sass / @wangeditor | 格式化、防抖节流、进度条、样式、富文本编辑器依赖 |

---

## 3. 项目运行与访问

### 3.1 开发环境启动方式

- 后端（ASGI/Daphne 或 runserver）：

  ```bash
  python manage.py runserver 0.0.0.0:8000
  ```

  说明：`breeding_system/asgi.py` 已配置 `ProtocolTypeRouter`（HTTP + WebSocket），生产可改用：

  ```bash
  daphne -b 0.0.0.0 -p 8000 breeding_system.asgi:application
  ```

- 前端（Vite）：

  ```bash
  npm run dev -- --host 0.0.0.0 --port 5173
  ```

### 3.2 默认访问地址

| 项目 | 地址 |
| --- | --- |
| 前端管理界面 | http://localhost:5173/ |
| 后端 API 根路径 | http://localhost:8000/（返回服务状态 JSON） |
| WebSocket 刷新通道 | ws://localhost:8000/ws/refresh/ |
| Django 管理后台 | http://localhost:8000/admin/ |
| 媒体文件 | http://localhost:8000/media/...（DEBUG 下由 urls.py 挂载） |

### 3.3 后端路由总览

根路由配置在 `breeding_system/urls.py`：

| 前缀 | 模块 | 说明 |
| --- | --- | --- |
| `/` | 根路径 | 返回系统状态与模块入口 JSON |
| `/api/users/` | 用户模块 | 登录、刷新、注册、个人中心、认证、账户管理 |
| `/api/livestock/` | 牲畜管理 | 分类、区域、批次 |
| `/api/production/` | 生产管理 | 饲料、投喂、疫苗、疾病 |
| `/api/stats/` | 统计模块 | 管理员看板、养殖户看板 |
| `/api/traceability/` | 溯源模块 | 公开查询、后台管理 |
| `/admin/` | Django 管理后台 | 内置后台（部分模型已注册） |

### 3.4 API 接口总览（按模块）

| 接口 | 方法 | 权限 | 说明 |
| --- | --- | --- | --- |
| `/api/users/register/` | POST | 公开 | 注册（自动创建养殖户档案） |
| `/api/users/login/` | POST | 公开 | JWT 登录（自动识别 role） |
| `/api/users/token/refresh/` | POST | 公开 | 刷新 Access Token |
| `/api/users/profile/` | GET/PATCH/DELETE | 登录 | 用户列表（管理员全量/普通用户仅自己） |
| `/api/users/profile/info/` | GET | 登录 | 当前用户信息 |
| `/api/users/profile/update_info/` | PATCH | 登录 | 更新手机号 |
| `/api/users/profile/upload_avatar/` | POST | 登录 | 上传头像（生成缩略图） |
| `/api/users/profile/change_password/` | POST | 登录 | 修改密码 |
| `/api/users/profile/{id}/toggle_active/` | POST | 管理员 | 启用/禁用用户 |
| `/api/users/farmer/` | GET | 管理员 | 养殖户档案列表 |
| `/api/users/cert/` | GET/POST | 登录 | 认证申请（提交/列表） |
| `/api/users/cert/my_cert/` | GET | 养殖户 | 我的认证状态 |
| `/api/users/cert/{id}/audit/` | POST | 管理员 | 审核认证 |
| `/api/livestock/category/` | GET/PUT | 登录读/管理员改 | 牲畜大类 |
| `/api/livestock/area/` | CRUD | 所有权 | 养殖区域 |
| `/api/livestock/batch/` | CRUD | 所有权 | 牲畜批次 |
| `/api/production/feed/` | CRUD | 所有权 | 饲料档案与库存 |
| `/api/production/feeding/` | CRUD | 所有权 | 投喂记录（联动库存） |
| `/api/production/vaccination/` | CRUD | 所有权 | 疫苗接种记录 |
| `/api/production/disease/` | CRUD | 按角色 | 疾病上报（含 assign/audit） |
| `/api/stats/admin/dashboard/` | GET | 管理员 | 管理端统计看板 |
| `/api/stats/farmer/dashboard/` | GET | 养殖户 | 养殖户统计看板 |
| `/api/traceability/public/{trace_code}/` | GET | 公开 | 公众溯源查询 |
| `/api/traceability/admin/` | GET/DELETE | 管理员 | 溯源记录管理 |

> 所有列表接口统一支持 `page`、`page_size` 分页参数（默认 10、上限 100），并支持 `search` 搜索与 `ordering` 排序（后端全局配置）。

---

## 4. 项目架构与目录结构

```
DjangoProject/
├── manage.py                     # Django 管理脚本
├── requirements.txt              # 后端依赖
├── breeding_system/              # 项目配置包
│   ├── settings.py               # 全局配置（DRF/JWT/CORS/Channels/媒体）
│   ├── urls.py                   # 根路由 + 媒体挂载
│   ├── asgi.py                   # ASGI 入口（HTTP + WebSocket）
│   └── wsgi.py                   # WSGI 入口
├── apps/                         # 业务应用（模块化）
│   ├── core/                     # 公共基础设施：分页、WS、信号、视图基类、工具、审核模型
│   ├── users/                    # 用户、角色、认证、个人中心
│   ├── livestock/                # 牲畜分类、区域、批次
│   ├── production/               # 饲料、投喂、疫苗、疾病
│   ├── stats/                    # 统计看板
│   └── traceability_app/         # 质量溯源（公开查询 + 后台管理）
├── media/                        # 用户上传（头像/证照/批次/饲料/区域/溯源码二维码）
│   ├── avatars/  certs/  batches/  feeds/  areas/  trace_qr_codes/
├── static/                       # 静态资源
└── frontend/                     # Vue3 前端
    ├── vite.config.ts            # Vite 配置（代理、分包）
    ├── package.json
    └── src/
        ├── main.ts / App.vue
        ├── router/index.ts       # 路由 + 角色守卫
        ├── store/                # Pinia（user、cert_manage）
        ├── utils/                # request、websocket、format、mask_id_card、image_upload
        ├── hooks/                # useCRUD、useDataRefresh
        ├── layout/index.vue      # 主布局（侧边菜单 + 顶栏）
        └── views/                # 各功能页面
```

---

## 5. 角色与权限模型

### 5.1 角色识别

| 角色 | 识别方式 | 说明 |
| --- | --- | --- |
| 管理员（admin） | `User.is_staff = True` | 拥有全局监管权限 |
| 兽医（vet） | 用户所属分组 `name = "vet"` 且不允许 `is_staff` | 仅处理分配给自己的疾病 |
| 养殖户（farmer） | 非管理员且不属于 vet 分组 | 业务数据主要来源 |

### 5.2 认证状态（养殖户实名）

- 养殖户档案 `FarmerProfile.is_verified` 表示是否已通过实名认证。
- **关键写操作**（新增/修改/删除业务数据）通常要求“已认证养殖户”。
- 认证申请本身允许未认证养殖户提交（`CertificationViewSet.create` 仅要求登录且为养殖户）。

### 5.3 自定义权限类（`apps/users/permissions.py`）

| 权限类 | 规则 |
| --- | --- |
| `IsAdminUser` | 登录且 `is_staff` |
| `IsFarmerUser` | 登录、非 staff、不属于 vet 分组 |
| `IsVerifiedFarmer` | 登录、非 staff、且 `farmer_profile.is_verified = True` |
| `IsVetUser` | 登录、非 staff、属于 vet 分组 |
| `DenyAll`（`apps/core/utils.py`） | 恒拒绝，用于禁止管理员创建/修改业务数据 |

### 5.4 基础视图集封装（`apps/core/utils.py`）

- **`BasePublicViewSet`**：登录用户可读（list/retrieve）；写操作（增删改）仅管理员可用；写后自动发送 WebSocket 刷新信号。用于“模板类”资源（如牲畜大类）。
- **`BaseOwnerViewSet`**：核心所有权视图集——
  - 管理员可查看全部数据；养殖户仅查看自身数据（按 `owner_field` 自动过滤）。
  - 创建时自动关联当前养殖户（`perform_create` 填充 `farmer`）。
  - 更新/删除前校验所有权，非本人数据返回 403。
  - 养殖户写操作需 `IsVerifiedFarmer`；管理员默认禁止创建与修改业务数据（除非子类覆盖或单独放行）。
- 两个基类均统一接入 `DjangoFilterBackend + SearchFilter + OrderingFilter`，写操作后自动调用 `send_refresh_signal`。

---

## 6. 功能清单（按角色）

### 6.1 管理员功能

#### 6.1.1 身份认证（管理员登录）
- **功能点**：管理员登录获取 JWT，登录响应自动识别 `role=admin`；支持账号禁用校验（被禁用账号返回 401 提示）。
- **后端接口**：`POST /api/users/login/`
- **后端实现**：`breeding_system/settings.py`（REST_FRAMEWORK + SIMPLE_JWT）、`apps/users/urls.py`、`apps/users/views.py`（`CustomTokenObtainPairView`）、`apps/users/serializers.py`（`CustomTokenObtainPairSerializer`：登录前检查 `is_active`，登录后按 `is_staff` / vet 分组 / 默认 判定角色写入响应）。
- **前端实现**：`frontend/src/views/login/index.vue`、`frontend/src/store/user.ts`、`frontend/src/utils/request.ts`
- **技术**：DRF + SimpleJWT + Pinia + Axios

#### 6.1.2 首页与统计（管理端看板）
- **功能点**：统计总养殖户数、在养批次、待处理疾病、免疫覆盖率，以及近 30 日疾病上报趋势（Pandas 按日重采样补零）。
- **后端接口**：`GET /api/stats/admin/dashboard/`
- **后端实现**：`apps/stats/urls.py`、`apps/stats/views.py`（`AdminDashboardStatsView`，权限 `IsAdminUser`）
- **前端实现**：`frontend/src/views/stats/index.vue`（ECharts 趋势图）、路由 `/stats`
- **技术**：DRF APIView + Pandas + ECharts

#### 6.1.3 账户管理（用户列表、编辑、禁用/启用、删除）
- **功能点**：管理员查看全部用户（支持搜索用户名/手机号）；可编辑、禁用/启用、删除用户；不能禁用自己。
- **后端接口**：`/api/users/profile/`、`/api/users/profile/{id}/toggle_active/`（POST）、PATCH/DELETE `/api/users/profile/{id}/`
- **后端实现**：`apps/users/views.py`（`UserProfileViewSet` + `toggle_active`）、`apps/users/serializers.py`（`UserDetailSerializer`，含 `role` 与 `profile` 嵌套）
- **前端实现**：`frontend/src/views/system/users/index.vue`、路由 `/system/users`
- **技术**：DRF ViewSet + django-filter + `IsAdminUser`

#### 6.1.4 养殖户管理（养殖户列表）
- **功能点**：管理员查看所有养殖户档案（用于监管与统计），支持按姓名/身份证/地址搜索。
- **后端接口**：`GET /api/users/farmer/`
- **后端实现**：`apps/users/views.py`（`FarmerViewSet`，`ReadOnlyModelViewSet` + `IsAdminUser`）、`apps/users/serializers.py`（`FarmerProfileSerializer`，身份证脱敏输出）
- **前端实现**：`frontend/src/views/system/users/index.vue`（结合用户管理页展示）
- **技术**：DRF ReadOnlyModelViewSet

#### 6.1.5 认证审核（审核养殖户实名）
- **功能点**：管理员查看认证申请（按状态分 Tab：待审核/通过/驳回，支持搜索养殖户姓名），审核通过/驳回并填写备注；**审核通过时同步 `FarmerProfile.is_verified=True` 并将认证身份证号同步至档案**，同时广播刷新以更新认证图标。
- **后端接口**：`/api/users/cert/`（GET，`filterset_fields=['status']`）、`/api/users/cert/{id}/audit/`（POST，status=1 通过 / 2 驳回）
- **后端实现**：`apps/users/views.py`（`CertificationViewSet.audit`）、`apps/users/models.py`（`Certification`/`FarmerProfile`/`StatusMixin`）、`apps/users/serializers.py`（`CertificationSerializer`，`id_card_full` 仅管理员可取明文）
- **前端实现**：`frontend/src/views/user/cert.vue`（管理员视角 Tab 页）、`frontend/src/store/cert_manage.ts`（多 Tab 预加载/搜索/竞态控制）、路由 `/system/cert`
- **技术**：DRF @action + 审核状态模型 + 图片上传（MEDIA）+ AbortController 竞态控制

#### 6.1.6 疾病协同（指派兽医）
- **功能点**：管理员查看全部疾病上报，将待处理记录指派给兽医（校验目标用户确为兽医），指派后状态保持待处理。
- **后端接口**：`/api/production/disease/`、`/api/production/disease/{id}/assign/`（POST）
- **后端实现**：`apps/production/views.py`（`DiseaseReportViewSet.assign`）、`apps/production/serializers.py`
- **前端实现**：`frontend/src/views/production/disease.vue`（分配弹窗选择兽医）
- **技术**：RBAC（`IsAdminUser`）+ DRF @action

#### 6.1.7 数据监管（全局业务数据只读监管）
- **功能点**：管理员对饲料、投喂、疫苗、区域、批次等数据进行全量查询与监管（管理员可看全部养殖户数据，默认禁止创建与修改业务数据）。
- **后端接口**：`/api/production/feed/`、`/api/production/feeding/`、`/api/production/vaccination/`、`/api/livestock/area/`、`/api/livestock/batch/`、`/api/livestock/category/`
- **后端实现**：`apps/core/utils.py`（`BaseOwnerViewSet` 权限与所有权过滤）、`apps/production/views.py`、`apps/livestock/views.py`
- **前端实现**：`frontend/src/views/production/*`、`frontend/src/views/livestock/*`（管理员视角显示养殖户列）
- **技术**：DRF + 统一分页/过滤/搜索/排序

#### 6.1.8 溯源管理（后台溯源记录管理）
- **功能点**：管理员查看/检索溯源记录（搜索溯源码、批次号；按养殖户、启用状态筛选）；可复制公开链接、查看/生成“溯源合格证”、删除溯源记录。
- **后端接口**：`/api/traceability/admin/`（GET/DELETE）
- **后端实现**：`apps/traceability_app/urls.py`、`apps/traceability_app/views.py`（`AdminTraceViewSet`）、`apps/traceability_app/serializers.py`（`AdminTraceSerializer`）
- **前端实现**：`frontend/src/views/system/trace/index.vue`、路由 `/system/trace`
- **技术**：DRF mixins + django-filter + 搜索 + qrcode.vue

#### 6.1.9 牲畜分类（基础模板维护）
- **功能点**：分类对登录用户可读；管理员可更新（仅限名称/编码约束内）；创建与删除被禁止；首次访问自动补齐六畜默认数据（鸡A/鸭B/鹅C/牛D/羊E/猪F，含标准生长周期）；大类名称与大类码绑定校验。
- **后端接口**：`/api/livestock/category/`
- **后端实现**：`apps/livestock/views.py`（`LivestockCategoryViewSet`，`get_queryset` 中调用 `_ensure_default_categories` 事务补种）、`apps/livestock/serializers.py`（`LivestockCategorySerializer`，固定六畜映射校验）
- **前端实现**：`frontend/src/views/livestock/category.vue`
- **技术**：`BasePublicViewSet` + `DenyAll` + 事务补种 + 自定义校验

#### 6.1.10 Django 管理后台
- **功能点**：内置 `/admin/`，已注册牲畜大类/区域/批次模型（列表筛选、搜索）。
- **实现**：`apps/livestock/admin.py`（用户/生产/溯源模块 admin 留有可选注册注释）

---

### 6.2 养殖户功能

#### 6.2.1 用户注册
- **功能点**：注册账号（用户名+密码），**自动创建养殖户档案**（默认未认证，`is_verified=False`）。
- **后端接口**：`POST /api/users/register/`
- **后端实现**：`apps/users/views.py`（`RegisterView`，`AllowAny`）、`apps/users/serializers.py`（`RegisterSerializer.create`）
- **前端实现**：`frontend/src/views/register/index.vue`、路由 `/register`
- **技术**：DRF CreateAPIView + Django ORM

#### 6.2.2 养殖户登录
- **功能点**：登录获取 JWT，响应自动识别 `role=farmer`。
- **后端接口**：`POST /api/users/login/`
- **后端实现**：`apps/users/views.py` + `apps/users/serializers.py`
- **前端实现**：`frontend/src/views/login/index.vue`、`frontend/src/store/user.ts`
- **技术**：SimpleJWT + Pinia

#### 6.2.3 个人中心（信息获取、更新、头像、改密）
- **功能点**：获取当前用户信息（含角色、档案、认证状态）；更新手机号；上传头像并生成缩略图（Pillow，约束文件名/大小/格式/像素安全）；修改密码（校验旧密码）。
- **后端接口**：`/api/users/profile/info/`、`/api/users/profile/update_info/`、`/api/users/profile/upload_avatar/`、`/api/users/profile/change_password/`
- **后端实现**：`apps/users/views.py`（`UserProfileViewSet` 各 @action）、`apps/users/serializers.py`（`UserDetailSerializer`）
- **前端实现**：`frontend/src/views/user/profile.vue`、`frontend/src/utils/image_upload.ts`（前端预校验）
- **技术**：DRF @action + Pillow + MEDIA 文件存储

#### 6.2.4 资格认证（实名申请与状态查询）
- **功能点**：提交认证申请（身份证号 + 身份证正反面 + 养殖许可证，**未认证也可提交**）；查看“我的认证状态”；被驳回后可重新提交。
- **后端接口**：`/api/users/cert/`（POST）、`/api/users/cert/my_cert/`（GET）
- **后端实现**：`apps/users/views.py`（`CertificationViewSet`）、`apps/users/models.py`（`Certification`/`FarmerProfile`）、`apps/users/serializers.py`（`CertificationSerializer`）
- **前端实现**：`frontend/src/views/user/cert.vue`（养殖户视角展示状态卡片与资料图片）
- **技术**：DRF + 图片上传 + 审核状态模型 + 身份证脱敏展示

#### 6.2.5 养殖档案（区域与批次）
- **功能点**：
  - 养殖区域管理：新增/编辑/删除自身区域；**删除时若该区域仍有在养批次则禁止删除**。
  - 牲畜批次管理：仅看本人；写操作需已认证；创建时系统自动生成批次号；支持筛选状态/区域/分类；管理员可查看全部。
- **后端接口**：`/api/livestock/area/`、`/api/livestock/batch/`
- **后端实现**：`apps/livestock/views.py`（`LivestockAreaViewSet`/`LivestockBatchViewSet`）、`apps/livestock/models.py`（`LivestockArea.delete` 校验、`LivestockBatch.save` 自动批次号）、`apps/core/utils.py`（`BaseOwnerViewSet`）
- **前端实现**：`frontend/src/views/livestock/area.vue`、`frontend/src/views/livestock/batch.vue`
- **技术**：`BaseOwnerViewSet` 所有权过滤 + 统一分页/筛选

#### 6.2.6 批次指标与生态评分
- **功能点**：批次卡片展示生长天数、标准周期、生长进度（%）、生态评分（1-5 星，可半星）、预计出栏日期、溯源码、溯源链接与二维码；出栏操作（PATCH status=finished）自动生成溯源记录；支持查看“溯源合格证”。
- **生态评分规则**（`LivestockBatch.eco_score`）：
  - 基础 3 分；
  - 有防疫记录 +1 分；
  - 无疾病记录 +1 分，有未康复疾病 -1 分（已康复不扣分）；
  - 最终取值裁剪到 1.0 ~ 5.0。
- **后端实现**：`apps/livestock/models.py`（`eco_score`）、`apps/livestock/serializers.py`（`growth_days`/`growth_progress`/`eco_score`/`expected_finish_date`/`trace_code`/`qr_code_image`/`trace_url`）、`apps/traceability_app/models.py`
- **前端实现**：`frontend/src/views/livestock/batch.vue`（进度条 + 星级评分 + 二维码）
- **技术**：模型属性 + 序列化器计算字段 + qrcode

#### 6.2.7 饲料管理（档案与库存）
- **功能点**：饲料档案增删改查（仅本人；写操作需已认证）；包含库存量、单位（默认 kg）、图片与简介；管理员视角可查看全部并显示养殖户。
- **后端接口**：`/api/production/feed/`
- **后端实现**：`apps/production/views.py`（`FeedInfoViewSet`）、`apps/production/models.py`（`FeedInfo`）、`apps/production/serializers.py`（`FeedInfoSerializer`）
- **前端实现**：`frontend/src/views/production/feed.vue`
- **技术**：DRF ViewSet + ORM + 图片上传校验

#### 6.2.8 投喂管理（库存一致性）
- **功能点**：新增/修改投喂记录自动扣减饲料库存；删除记录自动返还库存；校验投喂量 > 0、批次为在养状态、饲料与批次归属一致；**并发下库存不超卖**（行级锁 + 原子更新）。
- **后端接口**：`/api/production/feeding/`
- **后端实现**：`apps/production/serializers.py`（`FeedingRecordSerializer.create/update`：`transaction.atomic` + `select_for_update` + `F` 表达式，更新时同时锁定新旧饲料行）、`apps/production/views.py`（`FeedingRecordViewSet.perform_destroy` 回冲库存）
- **前端实现**：`frontend/src/views/production/feeding.vue`
- **技术**：`transaction.atomic` + `select_for_update` + `F`

#### 6.2.9 防疫管理（疫苗接种记录）
- **功能点**：疫苗接种记录增删改查（仅本人；写操作需已认证）；记录疫苗名称、接种日期、下次接种日期、备注；支持按批次筛选。
- **后端接口**：`/api/production/vaccination/`
- **后端实现**：`apps/production/views.py`（`VaccinationRecordViewSet`）、`apps/production/models.py`（`VaccinationRecord`）
- **前端实现**：`frontend/src/views/production/vaccination.vue`
- **技术**：DRF ViewSet

#### 6.2.10 疾病管理（上报与数量校验）
- **功能点**：养殖户上报疾病（选择批次、疾病名称、发病数量、症状）；**若上报状态为“死亡”则自动扣减批次数量并校验不为负**；删除“死亡”记录自动返还数量；支持按批次/状态筛选。
- **后端接口**：`/api/production/disease/`
- **后端实现**：`apps/production/serializers.py`（`DiseaseReportSerializer.create/update`：事务 + 行级锁 + `F` + 业务校验）、`apps/production/views.py`（`DiseaseReportViewSet.perform_destroy`）
- **前端实现**：`frontend/src/views/production/disease.vue`
- **技术**：`transaction.atomic` + `select_for_update` + `F` + 业务校验

#### 6.2.11 数据统计（养殖户看板）
- **功能点**：展示我的在养批次、待免疫批次、低库存饲料（≤50kg）数量、平均生态评分、近 30 日投喂趋势（按日聚合投喂量）、饲料库存分布（Top5 饼图）、近 30 日疾病趋势；**未认证返回提示信息**。
- **后端接口**：`GET /api/stats/farmer/dashboard/`
- **后端实现**：`apps/stats/views.py`（`FarmerDashboardStatsView`，权限 `IsFarmerUser`；未认证返回 `is_verified=False` 与提示文案）
- **前端实现**：`frontend/src/views/stats/index.vue`
- **技术**：Pandas 聚合 + ECharts + 权限控制

#### 6.2.12 首页看板（养殖户视角）
- **功能点**：`/dashboard` 首页整合“生长进度预警”（临近出栏批次）、智能待办/预警中心（待处理疾病等）、生态合规概览（防疫覆盖率、平均生态星级、健康预警）。
- **后端接口**：复用 `/api/livestock/batch/`、`/api/production/disease/`、`/api/stats/farmer/dashboard/` 等
- **前端实现**：`frontend/src/views/dashboard/index.vue`
- **技术**：Vue 组合 + Element Plus 进度/评分组件

#### 6.2.13 实时刷新
- **功能点**：数据变更后通知前端静默刷新列表/看板。
- **后端实现**：`apps/core/utils.py`（`send_refresh_signal`）、`apps/core/consumers.py`（`DataRefreshConsumer`，加入组 `data_refresh_group`）、`apps/core/routing.py`（`ws/refresh/`）、`breeding_system/asgi.py`、`apps/core/signals.py`（对 7 个业务模型注册 `post_save/post_delete` 广播）
- **前端实现**：`frontend/src/utils/websocket.ts`（自动重连/心跳/指数退避）、`frontend/src/hooks/useDataRefresh.ts`（消息节流 + 断线轮询补偿）
- **技术**：Django Channels + WebSocket + 轮询兜底

---

### 6.3 兽医功能

#### 6.3.1 兽医登录
- **功能点**：登录获取 JWT，响应自动识别 `role=vet`（通过用户分组判断）。
- **后端接口**：`POST /api/users/login/`
- **后端实现**：`apps/users/serializers.py`（角色识别）、`apps/users/permissions.py`（`IsVetUser`）
- **前端实现**：`frontend/src/views/login/index.vue`
- **技术**：SimpleJWT + Django Groups

#### 6.3.2 查看分配给我的疾病上报
- **功能点**：兽医仅能看到 `handled_by=本人` 的疾病上报记录。
- **后端接口**：`GET /api/production/disease/`
- **后端实现**：`apps/production/views.py`（`DiseaseReportViewSet.get_queryset` 按角色过滤：管理员全部 / 兽医仅本人 / 养殖户仅自己）
- **前端实现**：`frontend/src/views/production/disease.vue`
- **技术**：DRF 查询集按角色过滤

#### 6.3.3 处理/审核疾病上报
- **功能点**：兽医对指派给自己的记录进行处理，更新状态（待处理/治疗中/已康复/死亡）并填写处理意见；非本人记录禁止处理。
- **后端接口**：`POST /api/production/disease/{id}/audit/`
- **后端实现**：`apps/production/views.py`（`DiseaseReportViewSet.audit`）、`apps/users/permissions.py`（`IsVetUser`）
- **前端实现**：`frontend/src/views/production/disease.vue`（处理弹窗）
- **技术**：DRF @action + RBAC

#### 6.3.4 数据一致性（死亡/恢复影响数量）
- **功能点**：审核时状态在 `died` 与非 `died` 间切换，批次数量自动加减并保证不为负：
  - 非死亡 → 死亡：扣减批次数量（校验充足）；
  - 死亡 → 非死亡：返还数量；
  - 死亡 → 死亡（修改发病数）：按差值调整。
- **后端实现**：`apps/production/views.py`（`audit` 中 `transaction.atomic` + `select_for_update` + `F`）
- **技术**：`transaction.atomic` + `select_for_update` + `F`

#### 6.3.5 实时刷新
- **功能点**：处理完成后广播 `diseasereport` 刷新消息，驱动前端自动更新。
- **后端实现**：`apps/core/utils.py`（`send_refresh_signal`）+ 模型信号
- **前端实现**：`frontend/src/utils/websocket.ts`、`frontend/src/hooks/useDataRefresh.ts`
- **技术**：Django Channels + WebSocket

---

### 6.4 公众功能（无需登录）

#### 6.4.1 质量溯源查询（匿名访问）
- **功能点**：公众通过溯源码查询批次溯源信息：批次档案（品种、入栏/出栏日期、图片、简介）、产地环境（区域名称/介绍/图片）、养殖户名称与认证标识、投喂历史（近 10 条）、防疫历史（近 10 条）、疾病历史（近 10 条）、公开溯源链接；页面支持二维码展示与“已认证/核验中”主体资质标识。
- **后端接口**：`GET /api/traceability/public/{trace_code}/`
- **后端实现**：`apps/traceability_app/urls.py`、`apps/traceability_app/views.py`（`PublicTraceViewSet`，`AllowAny`，`lookup_field='trace_code'`，仅返回 `is_active=True` 记录）、`apps/traceability_app/serializers.py`（`PublicTraceDetailSerializer` 聚合序列化）
- **前端实现**：`frontend/src/views/trace/index.vue`、路由 `/public/trace/:code`
- **技术**：DRF ReadOnlyModelViewSet + 聚合序列化 + Vue 路由白名单

---

## 7. 关键实现要点（与功能强关联）

### 7.1 数据一致性与并发控制

- **饲料库存**随投喂记录增删改而变化：`transaction.atomic` + `select_for_update` 锁定行 + `F` 表达式原子增减；更新投喂记录时同时锁定新旧饲料行，防止并发超卖。
- **疾病“死亡”状态**影响批次当前数量：在创建（`DiseaseReportSerializer.create`）、更新（`update`）、兽医审核（`audit`）与删除（`perform_destroy`）四个环节均通过事务锁与校验保证数量不为负。

对应位置：`apps/production/serializers.py`、`apps/production/views.py`、`apps/production/models.py`、`apps/livestock/models.py`

### 7.2 隐私与信息展示控制

- 身份证号在应用层进行**简易加密存储**（`ENC_` 前缀 + Base64，生产建议升级为 AES），模型 `save` 时自动加密、避免重复加密。
- 展示规则按访问者身份区分：
  - 列表/详情接口统一输出**脱敏**号码（`mask_id_card`）；
  - 18 位：前 6 + 8 个 `*` + 后 4；15 位：前 6 + 5 个 `*` + 后 4；
  - **仅管理员**可通过认证审核接口的 `id_card_full` 字段获取明文。
- 前端 `mask_id_card.ts` 与后端规则保持一致，已含 `*` 的字符串不做二次处理。

对应位置：`apps/core/utils.py`（`encrypt_id_card/decrypt_id_card/mask_id_card`）、`apps/users/models.py`、`apps/users/serializers.py`、`frontend/src/utils/mask_id_card.ts`

### 7.3 实时刷新机制

- 后端在数据变更后向 Channels 组 `data_refresh_group` 广播 JSON（`type/data_type/message`）：
  - 视图基类写操作后手动广播；
  - `apps/core/signals.py` 对 `LivestockArea/LivestockBatch/LivestockCategory/FeedInfo/FeedingRecord/VaccinationRecord/DiseaseReport` 注册 `post_save/post_delete` 信号，实现“模型级兜底”广播。
- 前端通过 WebSocket 订阅消息：
  - 连接自动重连（指数退避，上限 8 次）、30s 心跳保活、页面可见时恢复连接；
  - 收到 refresh 消息后按 `data_type` 节流（120ms）触发列表静默刷新；
  - **断线补偿**：WS 不可用时以约 2.8s 间隔轮询兜底，保证最坏约 3s 内数据一致。

对应位置：`apps/core/utils.py`、`apps/core/consumers.py`、`apps/core/routing.py`、`apps/core/signals.py`、`breeding_system/asgi.py`、`frontend/src/utils/websocket.ts`、`frontend/src/hooks/useDataRefresh.ts`

### 7.4 批次号与溯源码生成

- **批次号（batch_code）**：创建批次时系统自动生成，格式 `{大类短码}{YYYYMMDD}{养殖户短码F0001}{顺序号}`（如 `A20260822F0001001`）；大类短码来自 `category_code`（无编码时按名称映射 鸡A/鸭B/鹅C/牛D/羊E/猪F，兜底 U）；顺序号按“当日同养殖户同大类”前缀取最大值 +1 补零（至少 3 位）；唯一约束冲突时事务内重试（最多 10 次）。
- **溯源码（trace_code）**：批次状态变为 `finished` 时自动创建/更新 `LivestockTrace`，溯源码格式 `ECO{时间戳}{6位随机大写HEX}`；随后自动生成二维码图片（`media/trace_qr_codes/trace_{code}.png`，容错 H 级），二维码内容为公开溯源页地址（支持 `TRACE_PUBLIC_BASE_URL` 环境变量覆盖，DEBUG 下自动使用本机局域网 IP + 5173 端口）。
- 批次序列化器额外输出 `trace_code`、`qr_code_image`、`trace_url` 供前端直接展示。

对应位置：`apps/livestock/models.py`（`LivestockBatch`）、`apps/traceability_app/models.py`（`LivestockTrace`）、`breeding_system/settings.py`（`TRACE_PUBLIC_BASE_URL`）

### 7.5 文件上传与媒体访问

- 头像/区域/批次/饲料/证件/二维码图片等统一进入 `MEDIA_ROOT`（`media/`），通过 `MEDIA_URL=/media/` 访问（DEBUG 下由 `urls.py` 的 `static()` 挂载）。
- 上传校验（后端 `get_image_upload_error` + 前端 `validate_image_file` 双重校验）：
  - 文件名字符集限制（中英文/数字/下划线/点/横线）；
  - 大小 ≤ 5MB；
  - 扩展名与 Content-Type 白名单：JPG/JPEG/PNG/GIF。
- 头像上传额外使用 Pillow 生成缩略图（控制 `MAX_IMAGE_PIXELS` 防解压炸弹），异常时安全回退。

对应位置：`breeding_system/settings.py`（MEDIA_ROOT/MEDIA_URL）、`apps/core/utils.py`、`apps/users/views.py`（`upload_avatar`）、`frontend/src/utils/image_upload.ts`

### 7.6 JWT 认证与 Token 自动刷新

- 后端：`SIMPLE_JWT` 配置 Access 1 天、Refresh 7 天、HS256；全局认证 `JWTAuthentication`；登录接口对禁用账号返回 401 提示。
- 前端：请求拦截器自动附加 `Authorization: Bearer <token>`；响应拦截器对 401 做**单飞刷新**——同一时刻仅发起一次 `/api/users/token/refresh/`，其余等待中的请求挂起排队，刷新成功后统一重放；刷新失败则清除 Token 并跳转登录页。

对应位置：`breeding_system/settings.py`、`apps/users/serializers.py`、`frontend/src/utils/request.ts`

### 7.7 前端路由守卫与角色控制

- 白名单：`/login`、`/register`、`/public/trace/:code`。
- 登录后按角色重定向根路径：管理员 → `/stats`，兽医 → `/production/disease`，养殖户 → `/dashboard`。
- 路由 `meta.role` 做页面级角色限制；侧边菜单按角色动态渲染（兽医隐藏饲料/投喂/疫苗等养殖户业务菜单，系统管理仅管理员可见）。
- 养殖户页面内“新增”按钮按认证状态禁用（未认证置灰提示）。

对应位置：`frontend/src/router/index.ts`、`frontend/src/layout/index.vue`

### 7.8 统一分页/过滤/搜索/排序

- 全局默认分页 `StandardPagination`：支持 `page`、`page_size`（默认 10、最大 100），响应结构 `{count, total_pages, current_page, page_size, results}`。
- 全局过滤后端：`DjangoFilterBackend + SearchFilter + OrderingFilter`；各 ViewSet 通过 `filterset_fields`、`search_fields` 声明可筛字段。

对应位置：`breeding_system/settings.py`、`apps/core/pagination.py`、各模块 views.py

---

## 8. 数据模型概览（数据库表）

| 表名 | 模型 | 关键字段 | 说明 |
| --- | --- | --- | --- |
| `users_user` | `User` | username, password, phone, avatar, is_staff, is_active | 扩展 AbstractUser |
| `users_admin_profile` | `AdminProfile` | user(1:1), real_name, department | 管理员资料 |
| `users_farmer_profile` | `FarmerProfile` | user(1:1), farmer_name, id_card(加密), address, is_verified | 养殖户档案 |
| `users_certification` | `Certification` | farmer(FK), id_card(加密), id_card_front/back, breeding_license, status, audit_remark, audit_time | 实名认证审核 |
| `livestock_category` | `LivestockCategory` | name, category_code(unique), icon, standard_cycle, description | 牲畜大类模板 |
| `livestock_area` | `LivestockArea` | farmer(FK), area_name, image, description | 养殖区域 |
| `livestock_batch` | `LivestockBatch` | category, area(PROTECT), farmer, batch_code(unique), quantity, entry_date, finish_date, status | 批次（核心表） |
| `production_feed` | `FeedInfo` | farmer, feed_name, image, unit, stock_quantity, created_at | 饲料与库存 |
| `production_feeding` | `FeedingRecord` | batch, feed(PROTECT), feed_weight, feeding_time, remark | 投喂记录 |
| `production_vaccine` | `VaccinationRecord` | batch, vaccine_name, vaccination_date, next_vaccination_date, remark | 疫苗接种 |
| `production_disease` | `DiseaseReport` | batch, disease_name, symptoms, affected_count, report_date, status, treatment_plan, handled_by, handled_at | 疾病上报 |
| `traceability_app_livestocktrace` | `LivestockTrace` | batch(1:1), trace_code(unique), is_active, created_at, qr_code_image | 溯源记录 |

> 另有 Django/Auth 内置表（`auth_group`、`auth_permission` 等）与各应用迁移记录表。数据表注释、字段 verbose_name 均已在模型中配置，可对照 Django Admin / 数据库注释查看。

---

